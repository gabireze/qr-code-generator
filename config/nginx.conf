# Configuração Nginx para qrcode.gabireze.cloud
# 
# Instalação:
# 1. Copie este arquivo: sudo cp config/nginx.conf /etc/nginx/sites-available/qrcode.gabireze.cloud
# 2. Ative o site: sudo ln -s /etc/nginx/sites-available/qrcode.gabireze.cloud /etc/nginx/sites-enabled/
# 3. Teste: sudo nginx -t
# 4. Recarregue: sudo systemctl reload nginx

# Redirecionar HTTP para HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name qrcode.gabireze.cloud www.qrcode.gabireze.cloud;
    
    # Redirecionar para HTTPS
    return 301 https://qrcode.gabireze.cloud$request_uri;
}

# Redirecionar www para não-www
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.qrcode.gabireze.cloud;
    
    # Certificados SSL (ajuste os caminhos conforme sua configuração)
    ssl_certificate /etc/letsencrypt/live/qrcode.gabireze.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/qrcode.gabireze.cloud/privkey.pem;
    
    return 301 https://qrcode.gabireze.cloud$request_uri;
}

# Configuração principal
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name qrcode.gabireze.cloud;
    
    # Diretório raiz do site
    root /var/www/qrcode.gabireze.cloud;
    index index.html;
    
    # Certificados SSL
    ssl_certificate /etc/letsencrypt/live/qrcode.gabireze.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/qrcode.gabireze.cloud/privkey.pem;
    
    # Configurações SSL modernas
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/rss+xml
        application/atom+xml
        image/svg+xml;
    
    # Cache para assets estáticos (nova estrutura: public/assets/)
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    location ~* \.(woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    location ~* \.(css|js)$ {
        expires 1M;
        add_header Cache-Control "public";
    }
    
    # HTML files (sem cache agressivo)
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }
    
    # Arquivos públicos da nova estrutura
    location /public/ {
        alias /var/www/qrcode.gabireze.cloud/public/;
    }
    
    # Robots.txt e sitemap.xml (nova localização: public/)
    location = /robots.txt {
        alias /var/www/qrcode.gabireze.cloud/public/robots.txt;
        access_log off;
        log_not_found off;
    }
    
    location = /sitemap.xml {
        alias /var/www/qrcode.gabireze.cloud/public/sitemap.xml;
        access_log off;
        log_not_found off;
    }
    
    # Manifest (nova localização: public/)
    location = /manifest.json {
        alias /var/www/qrcode.gabireze.cloud/public/manifest.json;
        add_header Cache-Control "public, max-age=3600";
    }
    
    # .htaccess (nova localização: public/)
    location = /.htaccess {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # Bloquear acesso a arquivos ocultos
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # Remover .html das URLs (SEO-friendly)
    # Redirecionar /pagina.html para /pagina
    if ($request_uri ~ ^/(.+)\.html$) {
        return 301 /$1;
    }
    
    # Página principal
    location / {
        # Tentar servir arquivo solicitado, depois com .html, senão 404
        try_files $uri $uri.html $uri/ =404;
    }
    
    # Logs
    access_log /var/log/nginx/qrcode.gabireze.cloud.access.log;
    error_log /var/log/nginx/qrcode.gabireze.cloud.error.log;
}
